import { RulerUnitTimePipe } from './ruler-unit-time.pipe';

describe('RulerUnitTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RulerUnitTimePipe();
    expect(pipe).toBeTruthy();
  });
});
