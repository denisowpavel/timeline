import { RulerUnitLabelPipe } from './ruler-unit-label.pipe';

describe('RulerUnitLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new RulerUnitLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
